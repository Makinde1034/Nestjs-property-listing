export enum ActivityEnum {
  ADDED = 'added',
  DELETED = 'deleted',
  CREATED = 'created',
  UPDATED = 'edited',
  VIEWED = 'viewed',
  DISABLE = 'disable',
  ENABLED = 'enabled',

  BLOCKED = 'blocked',
}
